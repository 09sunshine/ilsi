/**
 * Demo clock. The dataset describes the September 2026 cohort, so the
 * interface is rendered as if today were inside module 3.
 * Swap this for `new Date()` once the API provides live data.
 */
export const NOW = new Date("2026-11-05T10:00:00Z");
